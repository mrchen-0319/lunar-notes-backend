services:
  - type: web
    name: lunar-notes
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: VAPID_EMAIL
        value: mailto:lunar-notes@app.com
